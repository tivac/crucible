/* export anthracite */
var anthracite = {
    // Uncomment and add your firebase URL
    // firebase : "https://<YOUR-APP-HERE>.firebaseio.com/"
    
    // Other config params
    
    // Authentication type
    // auth : "password|jwt"
    
    // Login URL to use for jwt logins
    // loginBaseUrl : "http://some.url.here/for/logging/in/using-jwt"
    
    // Disable all write actions by setting locked to true
    // locked : true|false
    
    // Object containing default values for crucible fields
    // defaults : {
    //     String to use as the default start time for publishing,
    //     will use midnight if not specified
    //     publish_start_time : ""
    // }
};
